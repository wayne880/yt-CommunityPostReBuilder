# Documentation Assets

Place your preview screenshot here:

- `preview.png` - A screenshot of the HTML viewer interface

You can capture this by:
1. Running the archiver on any channel
2. Opening `archive-output/viewer/index.html` in a browser
3. Taking a screenshot of the interface
